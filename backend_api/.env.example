# Environment variables for FastAPI backend
GROQ_API_KEY=your_groq_api_key_here

# Note: Firebase credentials are loaded from ../.streamlit/secrets.toml
# No need to set FIREBASE_CREDENTIALS_PATH

# Server configuration
HOST=0.0.0.0
PORT=8000

# CORS origins (comma-separated)
ALLOWED_ORIGINS=*
